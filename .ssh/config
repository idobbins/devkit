# Portable SSH defaults. Put host aliases and secrets in ~/.ssh/config.local.
Include ~/.ssh/config.local

Host *
  ServerAliveInterval 30
  ServerAliveCountMax 3
  ExitOnForwardFailure yes
  ControlMaster auto
  ControlPath ~/.ssh/control-%C
  ControlPersist 10m
  AddKeysToAgent yes
